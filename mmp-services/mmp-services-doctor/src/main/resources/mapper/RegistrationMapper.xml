<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace 写的就是你要实现的那个接口的路径-->
<!--1.sql语句     2.绑定接口-->
<mapper namespace="com.aaa.doctor.dao.RegistredListDao">
    <!--查询使用的标签是select-->
    <!--id 代表的是对应的接口的方法-->
    <!--resultType 值 List  写的是List里面的泛型   Map-->
    <update id="handleSuccess">
        UPDATE his_registration hr set hr.registration_status =1 WHERE registration_id=#{registrationId}
    </update>

    <update id="handleReturn">
        UPDATE his_registration hr set hr.registration_status =4 WHERE registration_id=#{registrationId}
    </update>

    <update id="handleFalse">
        UPDATE his_registration hr set hr.registration_status =5 WHERE registration_id=#{registrationId}
    </update>


    <select id="getRegNumber" resultType="java.lang.Integer">
        SELECT MAX(registration_number) numberMax FROM his_registration WHERE dept_id=#{deptId}
    </select>

    <update id="updStatus">
        UPDATE his_registration hr set hr.registration_status =#{status} WHERE registration_id=#{registrationId}
    </update>


    <select id="selectSum" resultType="com.aaa.core.entity.Registration">
        SELECT user_id,doctor_name,SUM(registration_amount) sum,COUNT(*) count FROM his_registration
        <if test="doctorName!='' and doctorName!=null or dateRange !='' and dateRange > 0">
            where
        </if>
        <if test="doctorName!='' and doctorName!=null">
            doctor_name=#{param1,jdbcType=VARCHAR}
        </if>
        <if test="doctorName!='' and doctorName!=null and dateRange !='' and dateRange > 0">
            and
        </if>
        <if test="dateRange !='' and dateRange > 0">
            create_time between #{dateRange[0]} and #{dateRange[1]};
        </if>
        GROUP BY user_id,doctor_name
    </select>
</mapper>